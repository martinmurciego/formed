{
  "name": "formed",
  "version": "0.0.1",
  "description": "A form builder, with default, but customisable markup, which displays validation and errors",
  "main": "./index",
  "author": "Alex Barlow",
  "dependencies": {
    
  },
  "devDependencies": {
    "sequelize" : ">= 0.0.1"
  }
}